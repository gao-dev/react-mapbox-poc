import React from 'react';
import ReactDOM from 'react-dom';
import MapboxMap from './Mapbox/App';

ReactDOM.render(
  <React.StrictMode>
    <MapboxMap />
  </React.StrictMode>,
  document.getElementById('root')
);
